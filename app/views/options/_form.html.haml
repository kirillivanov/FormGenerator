= simple_form_for @option, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :value
  = f.input :field_id
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), options_path, :class => 'btn'
