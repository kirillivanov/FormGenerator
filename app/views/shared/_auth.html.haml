-unless user_signed_in?
  #signup(class='span3')
    = render 'devise/registrations/form'
  #signin(class='span5')
    = render 'devise/sessions/form'
- else
  = link_to "Logout", destroy_user_session_path, class: "btn btn-inverse", style: "float: right;"